package com.ai.dashboard.ai.prompt;

/**
 * Stable keys for prompt templates registered in the prompt management layer.
 */
public enum PromptType {
    TUTOR,
    HOMEWORK,
    LESSON_PLANNER,
    QUIZ,
    DOCUMENT_QA,
    SQL_GENERATOR
}
